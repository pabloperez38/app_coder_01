import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  lista: {
    marginBottom: 10,
    backgroundColor: "#fff",
    borderRadius: 5,
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.32,
    shadowRadius: 5.46,
    elevation: 5,
    padding: 10,
  },
  elemento: { fontSize: 16, paddingHorizontal: 10, paddingVertical: 10 },
});
